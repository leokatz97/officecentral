"""BBI Onboarding MCP — remote, authenticated MCP server wrapping the existing
push-* onboarding logic. DRAFT-only writes; guards enforced server-side; the
Shopify token is held in the server environment and never leaves it."""

__version__ = "0.1.0"
