# Shopping-Feed Readiness Audit + §4 Brand/Vendor Mis-Tag Fix

**Date:** 2026-06-05 · **Store:** office-central-online · **Scope:** full catalog (657 products)
**Author:** Claude Code (unattended-safe run) · **Status:** §4 HIGH fixes APPLIED + verified; everything else staged for review.

---

## 0. Preflight & feed-channel confirmation

| Check | Result |
|---|---|
| `shopify theme dev` watcher running | No (clean — Admin-API writes are theme-independent anyway) |
| Token `write_products` scope | **Present** (confirmed via `currentAppInstallation.accessScopes`) |
| Token `read_publications` scope | **Absent** — could not enumerate channels via API (see below) |
| Feed channel / Google app | **Google Shopping channel confirmed indirectly:** 466 products carry the `mm-google-shopping.google_product_category` metafield — the namespace written by Shopify's Google & YouTube channel / Merchant Center sync. The feed is generated by that channel. |

**Feed attribute → Shopify field mapping audited (Google & YouTube channel defaults):**

| GMC attribute | Shopify source field |
|---|---|
| `brand` | **product `vendor`** ← the §4 target |
| `id` / `mpn` | variant `sku` |
| `gtin` | variant `barcode` (empty catalog-wide — expected for furniture) |
| `title` | product `title` |
| `description` | `body_html` |
| `image_link` | featured image |
| `price` | variant `price` |
| `availability` | inventory + policy |
| `google_product_category` | `mm-google-shopping.google_product_category` metafield (or Shopify taxonomy `category`) |
| `product_type` | product `productType` |
| shipping weight / dimensions | `specs.weight` / `specs.dimensions` metafields (variant weight is 0 catalog-wide) |

> ⚠️ **One thing for Steve to confirm in Admin** (couldn't read via API): which products are actually *published to the Google channel*. This audit treats **ACTIVE products as the feed-eligible set** (596). ARCHIVED (61) are excluded from the feed by definition.

Audited against the **current** Google Merchant Center product data specification (fetched 2026-06-05): required = id, title (≤150), description (≤5000), link, image_link (min 500×500, no overlays/watermarks/borders), price, availability, brand (manufacturer, ≤70, never "Generic"/"N/A"); MPN required when no GTIN; condition defaults new.

---

## 1. FEED-READINESS REPORT

**Feed-eligible set = 596 ACTIVE products** (657 total − 61 archived).

| Feed status | Count | % of ACTIVE |
|---|---|---|
| ✅ READY | **84** | **14.1%** |
| ⚠️ MISSING-ATTRIBUTES | 389 | 65.3% |
| ⛔ DATA-ERROR | 117 | 19.6% |
| ⏭️ SERVICE-SKIP (delivery/installation line items — not feed products) | 6 | 1.0% |

### Gap counts by attribute (ACTIVE)

| Gap | Count | Severity for feed |
|---|---:|---|
| No shipping weight (`specs.weight` empty + variant weight 0) | 453 | quality/soft |
| No dimensions (`specs.dimensions` empty) | 311 | quality/soft |
| Image < 500×500 px | 160 | **block (enforced Jan 31 2027)** |
| No `google_product_category` | 107 | quality |
| **brand = "Brant Business Interiors"/blank** | 96 | **block** |
| No `product_type` AND no category at all | 89 | quality |
| No identifier (no GTIN, no SKU/MPN) | 59 | **block** |
| price ≤ $0 (B2B quote pages) | 23 | **block** (intentional — see below) |
| Link in description (`<a>`/URL in body) | 19 | policy risk |
| Thin/no description (<50 chars) | 12 | quality |
| No image at all | 7 | **block** |
| Title > 150 chars | 1 | block |

### The single biggest lever 🔑

**177 of the 389 MISSING-ATTRIBUTES products are blocked from READY by *nothing but* missing weight and/or dimensions.** Populating `specs.weight` + `specs.dimensions` on those would lift feed-ready from **84 → ~261 (14% → ~44%)** with no per-product brand/identifier judgment required. This is the highest-ROI remediation batch (see Punch-List #1).

### Worst offenders (multi-error)

Several "worst offenders" are **not real products** — they are option/placeholder pages that should be **excluded from the feed**, not fixed:
`please-select-a-finish`, `please-select-a-finish-1`, `colour`, `caster-options`, `installation-1`. Each has brand=BBI + no identifier + no image. (See Punch-List #6.)

---

## 2. §4 FIX LOG — vendor corrections APPLIED (live)

**6 HIGH-confidence vendor corrections applied** to the live catalog. Scope = **vendor field only** (the field that maps to the feed `brand` attribute). No title/body/tags/metafields/images/price touched.

Each write: full-state snapshot backup → `productUpdate(vendor)` → **hardened independent GraphQL readback, exact match**. All 6 readbacks PASS.

| # | Handle | SKU | Old vendor | → New vendor | Evidence | Readback |
|---|---|---|---|---|---|---|
| 1 | trapezoid-tables-3-sizes | `MYB-AT2448Z` | Brant Business Interiors | **MityBilt** | SKU prefix MYB = MityBilt (single-mfr) | ✅ exact |
| 2 | aktivity-puddle *(archived)* | `MYB-AT60PUD` | Brant Business Interiors | **MityBilt** | `specs.manufacturer` = MityBilt + MYB prefix | ✅ exact |
| 3 | ganging-plates-for-terina-tables | `GLC-GFT3072R` | Brant Business Interiors | **Global Furniture Group** | GLC = Global's own part-number prefix (parent manufacturer) | ✅ exact |
| 4 | **part-time-armless-task-chair-mvl2836** | `GLB-MVL2836` | **ergoCentric** | **OTG / Offices to Go** | MVL2836 is a documented Offices To Go model (officestogo.com); deterministic SKU overrides the corrupt brand metafield | ✅ exact |
| 5 | part-time-armless-posture-task-chair | `GLB-MVL2837` | Office Central & Brant Business Interiors | **OTG / Offices to Go** | MVL2837 = OTG Part-Time sibling (officestogo.com) | ✅ exact |
| 6 | progrid-mesh-back-managers-chair-freeflex-ii | `OSP-92553` | Office Central & Brant Business Interiors | **Office Star Products** | SKU prefix OSP = Office Star Products (single-mfr) | ✅ exact |

**Backups:** `data/backups/feed-vendor-<handle>-pre-20260605-104452.json` (one per product, gitignored).
**Log:** `data/logs/feed-vendor-corrections-20260605-104452.log` (gitignored).

### ⚠️ Residual to clean up on the two Part-Time chairs (MVL2836/MVL2837)

The §4 write corrected the **vendor only** (the feed brand field). On `part-time-armless-task-chair-mvl2836` the **`brand:ergocentric` tag and `specs.manufacturer = "ergoCentric"` metafield are still stale** — they carried the same corruption. These mis-route the product into ergoCentric collections and feed the content error in Part 3. They were left untouched to keep the unattended write narrowly scoped to "vendor correction." **Recommend a follow-up (1-line) write:** set tag → `brand:offices-to-go`, `specs.manufacturer` → `OTG / Offices to Go (a division of Global Furniture Group)`.

### Why so few HIGH fixes (only 6)?

The 2026-05-30 brand-recovery run (Block 4 Session 3) already corrected **183** BBI-vendored products. The 6 here are the residue + the two newly-surfaced Part-Time mis-tags. The remaining 92 BBI-vendored ACTIVE products have **no resolving signal** (50 have no SKU at all; the rest carry undecoded prefixes) and are correctly held for Steve (§3 below).

---

## 3. AMBIGUOUS — for Steve's call (NOT auto-fixed)

Two buckets, both exported to **`data/reports/vendor-mistag-candidates-2026-06-05.csv`** (filter `classification`).

### 3a. NON-BBI-CONFLICT (5) — a human set a real, non-BBI brand that conflicts with the SKU/metafield evidence. Overriding needs your call.

| Handle | Current vendor | SKU | Evidence points to | Note |
|---|---|---|---|---|
| willow-bariatric-chair | Lesro Industries | `GLB-W530AUU…` | Global (GLB prefix) | Is this a Global Willow bariatric chair mis-vendored to Lesro, or a genuine Lesro SKU with an odd code? |
| pedestal-file-file-with-or-without-wheels | Heartwood Manufacturing | `GLBMVLM23FF` | Global/OTG (GLB-MVL pedestal) | GLB-MVL is Global's own part number; Heartwood also makes pedestals — confirm before override. **(the Heartwood item flagged in the brief)** |
| auditorium-lecture-hall-seating *(archived)* | Borgo | `BORGO` | `specs.manufacturer` says Global | vendor (Borgo) vs metafield (Global) disagree |
| nesting-training-table-8-colour-choices *(archived)* | Office Star Products | `OSP-84224` | `specs.manufacturer` says OTG | OSP prefix = Office Star, but metafield says OTG — pick one |
| height-adjustable-base-pneumatic | Uline | `RIC-500788530` | Richelieu (RIC prefix) | Uline is a reseller; RIC = Richelieu. Likely Richelieu but confirm |

> **Heartwood "Cleo/Athena" situation** referenced in the brief: no live product with those names surfaced in this snapshot. The Heartwood ambiguity that *did* surface is the `GLBMVLM23FF` pedestal above. If Cleo/Athena exist under different handles, point me at them and I'll re-scan.

### 3b. AMBIGUOUS brand=BBI (102) — vendor is BBI/blank with no resolving signal.

- 100 vendor = "Brant Business Interiors", 2 = "Office Central & Brant Business Interiors".
- **50 have no SKU at all** → nothing to decode; need Steve to supply manufacturer or archive.
- The rest carry **undecoded prefixes**: `SCN` (7 — already "TBD, Steve confirmation required" in `sku-prefix-lookup.yaml`), `UN` (4), `NLP` (3), `WS` (3), `BEL`/`BELAIR` (4), `INV` (2), plus singletons (`SANOMA`, `SOFT`, `DSQ`, `ERGOACCESS`, `BOSVLED`, …).
- All remain a feed **`brand` block** until resolved. Full list in the CSV.

---

## 4. REMEDIATION PUNCH-LIST (next batch — do NOT auto-fix; several need judgment/Steve input)

Prioritized by feed impact × count. Each is the *menu* for a follow-up session, not part of this run.

| # | Fix | Products | Impact | Effort / who | Notes |
|---|---|---:|---|---|---|
| **1** | **Populate `specs.weight` + `specs.dimensions`** | ~453 / ~311 | 🔑 **Lifts feed-ready 14% → ~44%** (177 products are blocked by *only* these) | Data sourcing — manufacturer spec sheets | Highest ROI. Verified-spec dataset already has dimensions/weight for the enriched set; extend it. |
| **2** | **Re-generate / upscale images < 500×500 → ≥ 500×500** | 160 | Block as of Jan 31 2027; quality now | Image pipeline (existing `scripts/generate-clean-product-images.py`) | Many are 485×485 AI images. Verify no overlay/watermark/border per GMC. |
| **3** | **Resolve the 92 remaining brand=BBI** (vendor) | 92 active | Block (`brand`) | Steve + decode | The §3b AMBIGUOUS list. 50 need a manufacturer supplied; SCN/UN/NLP prefixes need decoding. |
| **4** | **Add `google_product_category`** | 107 | Quality / disapproval risk | Bulk map by `productType`/collection | Furniture > Office Furniture subtree. 466 already set — extend the same mapping. |
| **5** | **Populate `product_type`** | 500 blank (89 have neither type nor category) | Quality | Bulk from collection membership | productType is sparse catalog-wide; google category partly compensates. |
| **6** | **Exclude non-product / option pages from the feed** | ~5+ | Removes guaranteed disapprovals | Steve | `please-select-a-finish`, `please-select-a-finish-1`, `colour`, `caster-options`, `installation-1` — these are option placeholders, not sellable products. Exclude from Google channel (don't "fix"). |
| **7** | **Add SKU/MPN to the 57 no-identifier products** | 57 | Block (`identifier_exists`) | Data | Furniture has no GTIN, so MPN (SKU) + brand is the identifier. No SKU = no identifier. |
| **8** | **Strip links from 19 descriptions** | 19 | GMC policy (no links in description) | Bulk regex + review | Verify each — some may be internal cross-links that should become plain text. |
| **9** | **Decide policy for 23 $0-price quote pages** | 23 | Block in feed | Steve | Per BBI rules these stay LIVE with a Request-a-Quote CTA. They are **legitimately not feed-eligible** — exclude from Google channel rather than assigning a fake price. |

---

## 5. Part 3 — source-of-truth + content consistency

### Dataset corrected ✅
- **`spec-audit-verified-specs-2026-06-03.json`**: MVL2836 "Part-Time" `brand` changed **`ergoCentric` → `OTG / Offices to Go`** (source domain officestogo.com + `GLB-MVL2836` SKU). JSON re-validated; **zero `ergoCentric` entries remain**.
- Full dataset scanned (125 entries) for brand-vs-SKU-prefix/source-domain contradictions — **MVL2836 was the only one.** Dataset otherwise clean.

### Content mislabel — FLAGGED for correction (not auto-edited; "never publish content")

The brief expected the #22 guide to be clean. **It is not** — but the affected content is all in **drafts (not yet live)**, so nothing live is wrong:

| File | Line | Issue |
|---|---|---|
| `data/content-drafts/22-canadian-office-furniture-manufacturers-guide.html` | 144 | "*ergoCentric builds made-to-order task chairs such as the Part-Time armless task chair*" — **Part-Time is an Offices To Go chair, not ergoCentric.** #22 is a DOCS-only draft (not published), so fix before publish. |
| `data/content-drafts/batch-ledger.md` | 299 | Lists "ergoCentric Part-Time made-to-order task chair" in a "verified set, clean" line — same mislabel. |
| `docs/runlogs/manufacturer-comparisons-2026-06-04.md` | 24, 57, 58 | Mfr-compare **Posts 3 & 4** (Keilhauer vs ergoCentric, Humanscale vs ergoCentric) use "ergoCentric Part-Time MVL2836" as the verified-PDP citation. These Bucket-2 posts are `published:false` drafts. **Steve's call:** the posts lean on Part-Time as *the* ergoCentric exemplar — substituting a real ergoCentric model changes the argument, so this needs a human decision, not an auto-edit. (Line 110 of the same runlog already flagged this.) |

No published/live content cites the mislabel. ✅

---

## Artifacts

| File | Contents |
|---|---|
| `data/reports/feed-readiness-2026-06-05.json` | Per-product feed evaluation + summary |
| `data/reports/vendor-mistag-candidates-2026-06-05.csv` | All 113 flagged (HIGH / AMBIGUOUS / NON-BBI-CONFLICT) |
| `data/reports/_catalog-feed-snapshot-2026-06-05.json` | Raw read-only catalog snapshot (gitignored) |
| `data/logs/feed-vendor-corrections-20260605-104452.log` | §4 write log w/ readbacks (gitignored) |
| `data/backups/feed-vendor-*-pre-20260605-104452.json` | Pre-write snapshots (gitignored) |
| `scripts/fetch-catalog-feed-audit.py` · `feed-audit-analyze.py` · `apply-vendor-mistag-fixes.py` | Fetch / analyze / apply |

**HALT** — §4 HIGH fixes applied + verified. Everything in §3 and §4-punch-list awaits Steve.
