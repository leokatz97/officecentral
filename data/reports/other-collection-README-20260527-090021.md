# Other Collection — Steve's Classification Worksheet (v2 with tuned recs)

**Generated:** 20260527-090021  |  **Total products:** 338  |  **Est time:** ~45 min

---

## Claude's confidence breakdown (v2 — tuned)

- HIGH: 53 (15%)  ← scan, type Y, move on
- MED:  183 (54%)   ← glance, usually Y
- LOW:  58 (17%)   ← actually look
- NONE: 44 (13%)  ← Claude couldn't guess

~69% of rows should be quick Y/glance work.

## Workflow

For each row, ONE of:

**A. Accept** — Type **Y** in `accept_recommendations`. Done.
**B. Override** — Leave accept blank, fill `override_sub_collection_1/2/3` with handle(s) from picklist.
**C. Skip** — Type **Y** in `leave_in_other` (keep as-is) OR `archive_this` (discontinue).

## Don't touch

- Titles, handles, descriptions (reference only)
- `new_product_type` (taxonomy is frozen)

## Open in Google Sheets

1. File → Import → Upload → `other-collection-products-20260527-090021-with-recs.csv`
2. View → Freeze → 1 row
3. Filter by `claude_confidence` — work HIGH → MED → LOW → NONE

Save as CSV when done → email to leo@venn.ca.
